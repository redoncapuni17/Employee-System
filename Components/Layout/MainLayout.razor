@inherits LayoutComponentBase
@inject NavigationManager Navigation

<div class="page">
    @if (!Navigation.Uri.Contains("/login") && !Navigation.Uri.Contains("/signup"))
    {
    <div class="sidebar">
        <NavMenu />
    </div>
    }
    <main>
        <div class="top-row px-4">
            <NavLink class="nav-link" href="login" Match="NavLinkMatch.All">
                <span class="btn btn-outline-success" aria-hidden="true">Sign in</span>
            </NavLink>

            <NavLink class="nav-link" href="signup" Match="NavLinkMatch.All">
                <span class="btn btn-outline-success" aria-hidden="true">Sign up</span>
            </NavLink>
        </div>

        <article class="content px-4  ">
            @Body
        </article>
    </main>
</div>

<div id="blazor-error-ui">
    An unhandled error has occurred.
    <a href="" class="reload">Reload</a>
    <a class="dismiss">🗙</a>
</div>
